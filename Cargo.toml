[package]
name = "tracedna"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Freeze a failing run into an editable capsule, then find out which code the failure needs"

[dependencies]
